CREATE TABLE tecnologia (
                            id BIGINT AUTO_INCREMENT PRIMARY KEY, -- H2 soporta AUTO_INCREMENT
                            nombre VARCHAR(255) NOT NULL,
                            descripcion VARCHAR(255) NOT NULL
);